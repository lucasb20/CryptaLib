#include <vector>
#include <iostream>

class ocorr;

void freqAnalysis(const char*);
void _swap(size_t &,size_t &);
void bubbleSort(std::vector<size_t>&,std::vector<char>&);
void _swap(char &, char &);